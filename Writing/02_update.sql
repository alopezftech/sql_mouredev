UPDATE users SET age = '21' WHERE user_id = 1; // Actualiza la edad del usuario con ID 1 a 21 años.

// DEFINICION DE UPDATE:

    => La cláusula UPDATE se utiliza en SQL para modificar registros existentes en una tabla.
