SELECT COUNT(age) FROM users ; // Consulta el número total de usuarios con edad no nula

SELECT COUNT(*) FROM users ; // Consulta el número total de usuarios

// DEFINICION DE COUNT:

   => La función COUNT se utiliza en una consulta SQL para contar el número de filas que cumplen con una condición específica.
