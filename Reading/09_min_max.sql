SELECT MAX(age) FROM users ; // Consulta la edad máxima de todos los usuarios
SELECT MIN(age) FROM users ; // Consulta la edad mínima de todos los usuarios

// DEFINICION DE MIN/MAX:
   
    => Las funciones MIN y MAX se utilizan en una consulta SQL para obtener el valor mínimo o máximo de una columna numérica.