SELECT * FROM table_name WHERE column1 >= 60; // Consulta todos los usuarios con edad mayor o igual a 60

// DEFINICION DE WHERE:

    => La cláusula WHERE se utiliza para filtrar registros en una consulta SQL y devolver solo aquellos que cumplen con una condición específica.
