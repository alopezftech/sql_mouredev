SELECT AVG(age) FROM users ; // Consulta la edad promedio de todos los usuarios

// DEFINICION DE AVG:

   => La función AVG se utiliza en una consulta SQL para calcular la media (promedio) de los valores de una columna numérica.
