SELECT COUNT(age) FROM users HAVING COUNT(age) > 3; // Filtra los grupos de usuarios que tienen más de 3 registros de edad.

// DEFINICION DE HAVING:

=> La cláusula HAVING se utiliza en SQL para filtrar los resultados de una consulta después de que se han aplicado las funciones de agregación, permitiendo condiciones en los grupos resultantes.