	SELECT MAX(age) AS 'Edad máxima' FROM users GROUP BY age; // Consulta la edad máxima de los usuarios agrupados por edad, utilizando un alias para la columna resultante
    S
    // DEFINICION DE GROUP BY:
    
   => La cláusula GROUP BY se utiliza en una consulta SQL para agrupar filas que tienen valores idénticos en columnas especificadas, permitiendo realizar funciones agregadas en cada grupo.